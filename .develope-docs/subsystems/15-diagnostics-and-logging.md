# 15 诊断、自检与日志

状态：候选

## 职责

检查平台能力、配置、模型连通性、随包工具、CA、目录权限和上下文完整性，并记录可脱敏、可定位问题的日志。

## 边界

- 确定性检查优先；LLM 深度检查只能补充，不能替代。
- 日志后端不应与 TUI 输出耦合。
- 各子系统返回结构化错误，本系统负责聚合与呈现建议。

## 设计要求

- 默认不记录 API key、完整请求头或敏感文件内容。
- 日志轮换和大小上限必须适合老机器。
- 自检区分警告、不可用能力与阻断性失败。

## 待讨论

日志默认位置、保留周期，以及上下文 XML 是否同时承担审计日志职责。
