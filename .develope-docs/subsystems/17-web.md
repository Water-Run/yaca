# 17 Web 界面

状态：暂缓

## 职责候选

未来通过本地 HTTP 服务提供浏览器界面，并复用与 CLI/TUI 相同的会话、权限、配置和上下文应用服务。

## 当前决定

- 不进入 v0.1 核心实现顺序。
- 当前 `web/` 继续作为占位，不在设计阶段制造半成品。
- 恢复设计时需要重新确认是否仍以 Internet Explorer 6 为最低浏览器目标。

## 必须保持的边界

Web 不能复制 Agent 循环、权限判定或上下文存储；它只能作为交互适配器。
