# 规格冻结问答队列（负责人）

更新日期：2026-08-10

状态：**主队列已完成**（2026-08-10）；一次一题流程结束

## 目的

主线已无“大包未答问卷”，但多处规格仍写着“仍待确认 / 混合状态待定 / 用户可见呈现未冻”。  
本队列只收录 **仍会影响用户保证、体验或安全语义** 的缺口；下列内容 **不进本队列**（归技术证明 / 规格展开，不反复问偏好）：

- 库版本、Win32 API、毫秒默认值、缓冲区大小、退避公式（除非证明失败需要产品退路）
- 已由 D-049..D-057 收口且仅缺机械展开的字段表
- Web 双线（D-058）实现细节（独立队列，不阻塞 v0.1 主线）

每次提问格式固定：

1. 问题（含选项）  
2. 通俗解释 + Pro / Cons  
3. 建议参照  

## 内部测绘摘要（缺口分类）

| 类别 | 例子 | 处理 |
| --- | --- | --- |
| A. 用户可见规则未冻 | hash 字母表/大小写；碰撞页；损坏近处同名 | **本队列** |
| B. 命令 grammar 用户面 | `.cautious` / `.queue` 参数；审批提示符 | **本队列** |
| C. 失败时的用户保证 | Context 超 hard limit；非 TTY Stage2/3 consent | **本队列** |
| D. 纯技术未冻 | 事件泵 ABI、XML 库、curl carrier | **TP / owner 规格展开**，不问偏好 |
| E. 已有决定待展开 | AgentLoop 状态表、Config catalog | **规格写作**，不重问 A/B/C |

## 题量盘点（2026-08-10，含答复影响）

| 口径 | 数量 |
| --- | ---: |
| 初始主队列槽位 | **12**（SQ-01..SQ-12） |
| 已关闭 | **12**（主队列全部关闭；含 SQ-12→D-069 不展开退路） |
| **确定仍待问** | **0** |
| 条件题 | 全关（C5 不打开） |
| 已完成的负责人输入次数 | **11**（含 SQ-12 关闭指示） |
| 本队列剩余 | **0** — **主队列完成** |

### 条件题（受答复影响，默认不问）

| ID | 触发 | 主题草稿 |
| --- | --- | --- |
| SQ-C1 | SQ-05=A 已触发；**默认并入 CLI registry 技术冻结**（不强制再问） | online-consent 的 argv 唯一拼写 |
| SQ-C2 | ~~SQ-06 强制 compact~~ | **不打开**（D-063：触顶主路径=新开对话+接盘 Prompt） |
| SQ-C3 | ~~SQ-08 复杂 grammar~~ | **不打开**（D-065 status 已含 default/override/effective） |
| SQ-C4 | ~~跨 turn queue id / XML~~ | **不打开**（D-066：未开始 queue 默认不入 XML；`#N` 会话内稳定） |
| SQ-C5 | ~~SQ-12 降级 XP~~ | **不打开**（D-069：不预设退路；假定 luainstaller 可 qualification） |

另：纯技术规格展开（AgentLoop 状态表、字段 catalog 等）**不计入**本问卷；Web 双线另册。

## 拟定提问顺序（会随答复调整）

| ID | 主题 | 状态 | 主要挡住 |
| --- | --- | --- | --- |
| SQ-01 | Context hash 用户可见形式 | **已答 B** → D-059 | P0-11 |
| SQ-02 | 近处同名损坏 | **已答 A** → D-060 | P0-11 |
| SQ-03 | 短名歧义 / 首个命中 | **已答→D-061**（取消歧义页） | P0-11 |
| SQ-04 | 混合同名状态 | **取消**（并入 D-060/061） | — |
| SQ-05 | 非 TTY 下 Stage 2/3 self-test consent | **已答 A** → D-062 | P0-13、P1-07 |
| SQ-06 | Context hard limit 用户可见结果 | **已答** → D-063（够大/异常/新开+接盘 Prompt） | P0-10、P1-06 |
| SQ-07 | approval / 管理 REPL 提示符 + 可选色 | **已答 A** → D-064 | P0-05、P0-13 |
| SQ-08 | `.cautious` grammar | **已答 A** → D-065 | P0-13、P0-09 |
| SQ-09 | queue 条目 ID 与上限 | **已答 A+默认9** → D-066 | P0-02、P0-13 |
| SQ-10 | 自动压缩用户可见程度 | **已答 A** → D-067 | P0-12 |
| SQ-11 | XML 对外承诺强度 | **已答 C** → D-068（内部格式；export 为主） | P0-10、P1-10 |
| SQ-12 | Win32 x86 发布失败产品退路 | **关闭** → D-069（不展开；假定可交付） | P0-16 |

当前题：**无**（主队列已完成）。下一阶段回到规格展开 / 技术证明，而非继续 SQ。

## 答复归档规则

- 原话记入本文件「已答」表，并投影到 `DECISIONS.md`（新 D 或细化既有 D）与对应 `subsystems/*`。
- 若答复实际是技术选择，改标 `technical-proof`，不伪造成产品决定。
- 一次只推进一题；上一题归档前不展开下一题正文选项（可预告编号）。

## 已答

| ID | 答复 | 决定 | 投影 |
| --- | --- | --- | --- |
| SQ-01 | **B**（大写 hex `0-9A-F`；显示大写；输入 `a-f` 规范化为大写） | D-059 | `11-context-indexing.md`；细化 D-024 |
| SQ-02 | **A**（近处同名损坏 → `MatchedUnavailable`，不自动连远处） | D-060 | `11-context-indexing.md`；细化 D-024 |
| SQ-03 前提 | 短名**首个命中**；精准指定**只用 hash**；hash=整条逻辑路径 | **D-061** | 修订 D-024；重写 `11-context-indexing` 解析流；取消短名 `AmbiguousName` 题 |
| SQ-05 | **A**（非 TTY Stage≥2 无显式 online-consent → 硬失败；Stage1 可跑；禁止配置静默联网） | **D-062** | `13-cli.md`、`15-diagnostics-and-logging.md` |
| SQ-06 | HardLimit 足够大（触顶=异常）；fail-stop；提醒新开对话；建议接盘 Prompt 供下一 LLM 读关键内容 | **D-063** | `10-context-storage.md`、`12-context-compaction.md` |
| SQ-07 | **A** 分焦点提示符 + 可选基础色（`>>` 浅色、`model>` 绿等）；XP 可降级无色 | **D-064** | `14-tui.md`、`13-cli.md` |
| SQ-08 | **A**（`.cautious` / on / off / toggle / reset；无参=status） | **D-065** | `14-tui.md` |
| SQ-09 | **A** + 配置最大条数默认 **9**；`#N` 稳定；clear 后从 1 重计；队满拒增 | **D-066** | `09-agent-session`、`05-configuration`、`14-tui` |
| SQ-10 | **A** 自动 compact：STATUS 可见、不打断确认、禁止成功静默 | **D-067** | `12-context-compaction`、`14-tui` |
| SQ-11 | **C** XML=内部格式；不承诺第三方 R/W API；跨工具靠 export | **D-068** | `10-context-storage` |
| SQ-12 | **不考虑退路**；假定 luainstaller 能支撑含 XP 的三包 | **D-069** | 维持 D-007/D-056；TP-001 仍要证据 |
