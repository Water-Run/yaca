# 开发交接（2026-09-14）

本次为额度重置前的短窗口自主推进（约 10:26–11:00 JST），
目标是把「Context 管理交互」节点从只读调查推进到可执行状态。
阶段仍为 `implemented-unqualified`，Gate A/B 通过，Release Gate R 关闭。

## 本次完成

| 提交 | 内容 |
| --- | --- |
| `b10b044` | 产出 [Context 管理交互实现计划](CONTEXT-REPL-PLAN.md)，节点切分为 N1/N2/N3 |
| `7a34715` | 修复 `new_model_setup_input` 的取消码误归因，登记 `Context` 标签 |
| `7d893f3` | 补记本轮 TRACKING，并更正过时的 design-contract 计数 |
| `cffca4e` | 实测后更正测试夹具缺口的范围 |

### 唯一的源码改动（`7a34715`）

`new_model_setup_input` 原以 `label == "Configuration"` 二选一推导
`cancel_code`，其余标签一律落到 `ModelSetupCancelled`。新交互面复用该
helper 时，Esc/EOF 会报出 model setup 的错误码，属于跨子系统错误归因。
现改为显式 `SETUP_INPUT_CANCEL_CODES` 映射，未注册标签返回
`InvalidSetupInput` 而非静默回退；`Context` → `ContextReplCancelled`
已登记；两处调用点均已传播错误。

验证：完整 suite **481/481**；design-contract **7611**、
proof-evidence **56**、coding-readiness **553** 条断言 PASS。

## 必须知道的三件事

1. **未推送。** 四个提交都只在本地 `main`。本环境无 GitHub 凭据
   （`could not read Username for 'https://github.com'`）。
   恢复时先 `git push origin main`，推送前重新 fetch 复核。
2. **TP-008 本轮未重新证明。** 本机缺 `xmllint`，proof 在第一条断言即失败。
   这是环境缺口不是回归；资格评估前须在具备 `xmllint` 的环境重跑。
3. **design-contract 计数以 7611 为准。** 旧 TRACKING 记为 7612；
   本树在打补丁前后都报 7611（已用 `git stash` 对照确认），旧数字已过时。

## 下一步：N1 只读回路

按 [实现计划](CONTEXT-REPL-PLAN.md) 执行，第一项工作是端口替身，
**不需要新建加载器**——`src/main.lua:9590` 的 `YACA_TEST_ROOT` 守卫
已使 main.lua 可按普通模块加载，导出含 `run_config_repl` 等 10 个函数。

替身需满足的终端契约（`new_model_setup_input` 实际调用）：
`start(self, now)` / `poll(self, now, budget)` / `cancel(self, now)` /
`close(self)`，事件为 `{kind="user_action", action="text"|"cancel"|"eof",
text=...}` 与 `{kind="io_terminal"}`。可先用 `run_config_repl`
作参照面校准替身，再实现 `M.run_context_repl` 并在
`default_runtime_dispatch` 加入 `context-repl` 分支。

本次**未**实现任何 Context 回路代码，也未新增测试；
`new_model_setup_input` 的修复仅由完整 suite 证明无回归，无直接单元测试。

## 纪律

Git 沿用 [D-071](DECISIONS.md#d-071-编码就绪收尾与核心节点推送授权2026-08-29)：
直接在 `main` 工作，完整节点 fetch 复核后提交并推送。
验证一律经 `.tools/run_with_resource_guard.sh` 串行执行
（targeted → full suite → coding readiness/proofs），不复用旧的资源数字。
