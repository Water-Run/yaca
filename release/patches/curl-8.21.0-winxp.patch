--- a/configure
+++ b/configure
@@ -18801,8 +18801,8 @@
     *-DWINSTORECOMPAT*) curl_cv_winuwp='yes';;
   esac
 
-  { printf "%s\n" "$as_me:${as_lineno-$LINENO}: checking if building for Windows Vista or newer" >&5
-printf %s "checking if building for Windows Vista or newer... " >&6; }
+  { printf "%s\n" "$as_me:${as_lineno-$LINENO}: checking if building for Windows XP or newer" >&5
+printf %s "checking if building for Windows XP or newer... " >&6; }
   cat confdefs.h - <<_ACEOF >conftest.$ac_ext
 /* end confdefs.h.  */
 
@@ -18812,7 +18812,7 @@
 int main(void)
 {
 
-      #if (_WIN32_WINNT < 0x600)
+      #if (_WIN32_WINNT < 0x501)
       #error
       #endif
 
@@ -18830,7 +18830,7 @@
 
     { printf "%s\n" "$as_me:${as_lineno-$LINENO}: result: no" >&5
 printf "%s\n" "no" >&6; }
-    as_fn_error $? "Building for Windows Vista or newer is required." "$LINENO" 5
+    as_fn_error $? "Building for Windows XP or newer is required." "$LINENO" 5
 
 fi
 rm -f core conftest.err conftest.$ac_objext conftest.beam conftest.$ac_ext
@@ -30180,10 +30180,6 @@
   LIBS="-ladvapi32 -lcrypt32 $LIBS"
 fi
 
-if test "$curl_cv_native_windows" = "yes"; then
-    LIBS="-lbcrypt $LIBS"
-fi
-
 case "x$SSL_DISABLED$OPENSSL_ENABLED$GNUTLS_ENABLED$MBEDTLS_ENABLED$WOLFSSL_ENABLED$SCHANNEL_ENABLED$RUSTLS_ENABLED" in
   x)
     as_fn_error $? "TLS not detected, you will not be able to use HTTPS, FTPS, NTLM and more.
--- a/configure.ac
+++ b/configure.ac
@@ -625,12 +625,12 @@
     *-DWINSTORECOMPAT*) curl_cv_winuwp='yes';;
   esac
 
-  AC_MSG_CHECKING([if building for Windows Vista or newer])
+  AC_MSG_CHECKING([if building for Windows XP or newer])
   AC_COMPILE_IFELSE([
     AC_LANG_PROGRAM([[
       #include <windows.h>
     ]],[[
-      #if (_WIN32_WINNT < 0x600)
+      #if (_WIN32_WINNT < 0x501)
       #error
       #endif
     ]])
@@ -638,7 +638,7 @@
     AC_MSG_RESULT([yes])
   ],[
     AC_MSG_RESULT([no])
-    AC_MSG_ERROR([Building for Windows Vista or newer is required.])
+    AC_MSG_ERROR([Building for Windows XP or newer is required.])
   ])
 fi
 
@@ -2109,11 +2109,6 @@
   LIBS="-ladvapi32 -lcrypt32 $LIBS"
 fi
 
-if test "$curl_cv_native_windows" = "yes"; then
-  dnl for BCryptGenRandom()
-  LIBS="-lbcrypt $LIBS"
-fi
-
 case "x$SSL_DISABLED$OPENSSL_ENABLED$GNUTLS_ENABLED$MBEDTLS_ENABLED$WOLFSSL_ENABLED$SCHANNEL_ENABLED$RUSTLS_ENABLED" in
   x)
     AC_MSG_ERROR([TLS not detected, you will not be able to use HTTPS, FTPS, NTLM and more.
--- a/lib/curl_setup.h
+++ b/lib/curl_setup.h
@@ -160,11 +160,22 @@
 #endif /* HAVE_CONFIG_H */
 
 #ifdef _WIN32
+#  if defined(_WIN32_WINNT) && (_WIN32_WINNT < 0x0501)
+#    error The minimum build target is Windows XP (0x0501)
+#  endif
+
 #  if defined(_WIN32_WINNT) && (_WIN32_WINNT < 0x0600)
-#    error The minimum build target is Windows Vista (0x0600)
+#    ifdef USE_IPV6
+#      error Windows XP compatibility builds require IPv6 to be disabled
+#    endif
+#    ifdef USE_RESOLV_THREADED
+#      error Windows XP compatibility builds require the synchronous resolver
+#    endif
 #  endif
 
-#  if !defined(CURL_WINDOWS_UWP) && (defined(_MSC_VER) || defined(__MINGW32__))
+#  if !defined(CURL_WINDOWS_UWP) && \
+      (!defined(_WIN32_WINNT) || (_WIN32_WINNT >= 0x0600)) && \
+      (defined(_MSC_VER) || defined(__MINGW32__))
 #    ifndef HAVE_IF_NAMETOINDEX
 #    define HAVE_IF_NAMETOINDEX
 #    endif
--- a/lib/easy_lock.h
+++ b/lib/easy_lock.h
@@ -27,7 +27,7 @@
 
 #define GLOBAL_INIT_IS_THREADSAFE
 
-#ifdef _WIN32
+#if defined(_WIN32_WINNT) && (_WIN32_WINNT >= 0x0600)
 
 #define curl_simple_lock      SRWLOCK
 #define CURL_SIMPLE_LOCK_INIT SRWLOCK_INIT
@@ -72,6 +72,8 @@
       __builtin_ia32_pause();
 #elif defined(__aarch64__)
       __asm__ volatile("yield" ::: "memory");
+#elif defined(_WIN32)
+      Sleep(1);
 #elif defined(HAVE_SCHED_YIELD)
       sched_yield();
 #endif
--- a/lib/curl_threads.h
+++ b/lib/curl_threads.h
@@ -45,22 +45,31 @@
 #  define curl_mutex_t           CRITICAL_SECTION
 #  define curl_thread_t          HANDLE
 #  define curl_thread_t_null     (HANDLE)0
-#  define Curl_mutex_init(m)     InitializeCriticalSectionEx(m, 0, 1)
+#  if defined(_WIN32_WINNT) && (_WIN32_WINNT < 0x0600)
+#    define Curl_mutex_init(m)   InitializeCriticalSection(m)
+#  else
+#    define Curl_mutex_init(m)   InitializeCriticalSectionEx(m, 0, 1)
+#  endif
 #  define Curl_mutex_acquire(m)  EnterCriticalSection(m)
 #  define Curl_mutex_release(m)  LeaveCriticalSection(m)
 #  define Curl_mutex_destroy(m)  DeleteCriticalSection(m)
-#  define curl_cond_t            CONDITION_VARIABLE
-#  define Curl_cond_init(c)      InitializeConditionVariable(c)
-#  define Curl_cond_destroy(c)   (void)(c)
+#  if !defined(_WIN32_WINNT) || (_WIN32_WINNT >= 0x0600)
+#    define curl_cond_t          CONDITION_VARIABLE
+#    define Curl_cond_init(c)    InitializeConditionVariable(c)
+#    define Curl_cond_destroy(c) (void)(c)
+#  endif
 #else
 #error neither HAVE_THREADS_POSIX nor _WIN32 defined
 #endif
 
+#if defined(HAVE_THREADS_POSIX) || !defined(_WIN32_WINNT) || \
+  (_WIN32_WINNT >= 0x0600)
 void Curl_cond_signal(curl_cond_t *c);
 void Curl_cond_wait(curl_cond_t *c, curl_mutex_t *m);
 /* Returns CURLE_OPERATION_TIMEDOUT on timeout */
 CURLcode Curl_cond_timedwait(curl_cond_t *c, curl_mutex_t *m,
                              uint32_t timeout_ms);
+#endif
 #endif /* USE_MUTEX */
 
 #ifdef USE_THREADS
--- a/lib/curl_threads.c
+++ b/lib/curl_threads.c
@@ -186,7 +186,8 @@
   return rc ? CURLE_UNRECOVERABLE_POLL : CURLE_OK;
 }
 
-#elif defined(_WIN32)
+#elif defined(_WIN32) && \
+  (!defined(_WIN32_WINNT) || (_WIN32_WINNT >= 0x0600))
 
 void Curl_cond_signal(CONDITION_VARIABLE *c)
 {
@@ -208,6 +209,9 @@
   }
   return CURLE_OK;
 }
+#elif defined(_WIN32)
+/* The Windows XP profile is synchronous and does not build a condition
+ * variable consumer. Its critical-section mutex remains available. */
 #else
 #error neither HAVE_THREADS_POSIX nor _WIN32 defined
 #endif
--- a/lib/rand.c
+++ b/lib/rand.c
@@ -34,20 +34,33 @@
 #include "escape.h"
 
 #ifdef _WIN32
-#include <bcrypt.h>
-#ifndef STATUS_SUCCESS
-#define STATUS_SUCCESS ((NTSTATUS)0x00000000L)
+#ifdef USE_WIN32_CRYPTO
+#include <wincrypt.h>
 #endif
 
 CURLcode Curl_win32_random(unsigned char *entropy, size_t length)
 {
   memset(entropy, 0, length);
 
-  if(BCryptGenRandom(NULL, entropy, (ULONG)length,
-                     BCRYPT_USE_SYSTEM_PREFERRED_RNG) != STATUS_SUCCESS)
-    return CURLE_FAILED_INIT;
+#ifdef USE_WIN32_CRYPTO
+  {
+    HCRYPTPROV provider = 0;
+
+    if(length > UINT_MAX ||
+       !CryptAcquireContextW(&provider, NULL, NULL, PROV_RSA_FULL,
+                             CRYPT_VERIFYCONTEXT | CRYPT_SILENT))
+      return CURLE_FAILED_INIT;
+    if(!CryptGenRandom(provider, (DWORD)length, entropy)) {
+      CryptReleaseContext(provider, 0UL);
+      return CURLE_FAILED_INIT;
+    }
+    CryptReleaseContext(provider, 0UL);
+  }
 
   return CURLE_OK;
+#else
+  return CURLE_NOT_BUILT_IN;
+#endif
 }
 #endif
 
--- a/lib/curlx/timeval.c
+++ b/lib/curlx/timeval.c
@@ -26,25 +26,36 @@
 #ifdef _WIN32
 
 #include "system_win32.h"
+#include "version_win32.h"
 
 LARGE_INTEGER Curl_freq;
+static bool use_qpc;
 
 /* For tool or tests, we must initialize before calling curlx_now().
    Providing this function here is wrong. */
 void curlx_now_init(void)
 {
+  use_qpc = curlx_verify_windows_version(6, 0, 0, PLATFORM_WINNT,
+                                         VERSION_GREATER_THAN_EQUAL);
   QueryPerformanceFrequency(&Curl_freq);
 }
 
 /* In case of bug fix this function has a counterpart in tool_util.c */
 void curlx_pnow(struct curltime *pnow)
 {
-  LARGE_INTEGER count;
-  DEBUGASSERT(Curl_freq.QuadPart);
-  QueryPerformanceCounter(&count);
-  pnow->tv_sec = (time_t)(count.QuadPart / Curl_freq.QuadPart);
-  pnow->tv_usec = (int)((count.QuadPart % Curl_freq.QuadPart) * 1000000 /
-                        Curl_freq.QuadPart);
+  if(use_qpc) {
+    LARGE_INTEGER count;
+    DEBUGASSERT(Curl_freq.QuadPart);
+    QueryPerformanceCounter(&count);
+    pnow->tv_sec = (time_t)(count.QuadPart / Curl_freq.QuadPart);
+    pnow->tv_usec = (int)((count.QuadPart % Curl_freq.QuadPart) * 1000000 /
+                          Curl_freq.QuadPart);
+  }
+  else {
+    DWORD milliseconds = GetTickCount();
+    pnow->tv_sec = (time_t)(milliseconds / 1000);
+    pnow->tv_usec = (int)((milliseconds % 1000) * 1000);
+  }
 }
 
 #elif defined(HAVE_CLOCK_GETTIME_MONOTONIC) || \
--- a/lib/curlx/fopen.c
+++ b/lib/curlx/fopen.c
@@ -126,17 +126,30 @@
 
 #ifndef _UNICODE
   /* convert multibyte input to unicode */
+#if defined(_WIN32_WINNT) && (_WIN32_WINNT < 0x0600)
+  count = mbstowcs(NULL, in, 0);
+  if(count == (size_t)-1 || count >= max_path_len)
+    goto cleanup;
+  needed = count + 1;
+#else
   if(mbstowcs_s(&needed, NULL, 0, in, 0))
     goto cleanup;
   if(!needed || needed >= max_path_len)
     goto cleanup;
+#endif
   ibuf = CURLX_MALLOC(needed * sizeof(wchar_t));
   if(!ibuf)
     goto cleanup;
+#if defined(_WIN32_WINNT) && (_WIN32_WINNT < 0x0600)
+  count = mbstowcs(ibuf, in, needed);
+  if(count == (size_t)-1 || count != needed - 1)
+    goto cleanup;
+#else
   if(mbstowcs_s(&count, ibuf, needed, in, needed - 1))
     goto cleanup;
   if(count != needed)
     goto cleanup;
+#endif
   in_w = ibuf;
 #else
   in_w = in;
@@ -188,6 +201,10 @@
       if(!temp)
         goto cleanup;
 
+#if defined(_WIN32_WINNT) && (_WIN32_WINNT < 0x0600)
+      memcpy(temp, L"\\\\?\\UNC\\", 8 * sizeof(wchar_t));
+      memcpy(temp + 8, fbuf + 2, (count - 1) * sizeof(wchar_t));
+#else
       if(wcsncpy_s(temp, needed, L"\\\\?\\UNC\\", 8)) {
         CURLX_FREE(temp);
         goto cleanup;
@@ -196,6 +213,7 @@
         CURLX_FREE(temp);
         goto cleanup;
       }
+#endif
     }
     else {
       /* "\\?\" + full path + null */
@@ -207,6 +225,10 @@
       if(!temp)
         goto cleanup;
 
+#if defined(_WIN32_WINNT) && (_WIN32_WINNT < 0x0600)
+      memcpy(temp, L"\\\\?\\", 4 * sizeof(wchar_t));
+      memcpy(temp + 4, fbuf, (count + 1) * sizeof(wchar_t));
+#else
       if(wcsncpy_s(temp, needed, L"\\\\?\\", 4)) {
         CURLX_FREE(temp);
         goto cleanup;
@@ -215,6 +237,7 @@
         CURLX_FREE(temp);
         goto cleanup;
       }
+#endif
     }
 
     CURLX_FREE(fbuf);
@@ -223,17 +246,30 @@
 
 #ifndef _UNICODE
   /* convert unicode full path to multibyte output */
+#if defined(_WIN32_WINNT) && (_WIN32_WINNT < 0x0600)
+  count = wcstombs(NULL, fbuf, 0);
+  if(count == (size_t)-1 || count >= max_path_len)
+    goto cleanup;
+  needed = count + 1;
+#else
   if(wcstombs_s(&needed, NULL, 0, fbuf, 0))
     goto cleanup;
   if(!needed || needed >= max_path_len)
     goto cleanup;
+#endif
   obuf = CURLX_MALLOC(needed);
   if(!obuf)
     goto cleanup;
+#if defined(_WIN32_WINNT) && (_WIN32_WINNT < 0x0600)
+  count = wcstombs(obuf, fbuf, needed);
+  if(count == (size_t)-1 || count != needed - 1)
+    goto cleanup;
+#else
   if(wcstombs_s(&count, obuf, needed, fbuf, needed - 1))
     goto cleanup;
   if(count != needed)
     goto cleanup;
+#endif
   *out = obuf;
   obuf = NULL;
 #else
@@ -386,7 +422,11 @@
       target = fixed;
     else
       target = filename_w;
+#if defined(_WIN32_WINNT) && (_WIN32_WINNT < 0x0600)
+    file = _wfreopen(target, mode_w, fp);
+#else
     errno = _wfreopen_s(&file, target, mode_w, fp);
+#endif
   }
   else
     /* !checksrc! disable ERRNOVAR 1 */
@@ -398,8 +438,12 @@
     target = fixed;
   else
     target = filename;
+#if defined(_WIN32_WINNT) && (_WIN32_WINNT < 0x0600)
+  file = freopen(target, mode, fp);
+#else
   errno = freopen_s(&file, target, mode, fp);
 #endif
+#endif
 
   CURLX_FREE(fixed);
   return file;
